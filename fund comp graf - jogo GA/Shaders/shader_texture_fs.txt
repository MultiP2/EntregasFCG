#version 460

in vec4 vertexColor;
in vec2 texCoord;

out vec4 color;

//pixels da textura
uniform sampler2D tex_buffer;
uniform vec2 offsets;

void main()
{
    color = texture(tex_buffer, texCoord + offsets);
}